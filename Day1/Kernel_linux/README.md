# Kernel linux driver solution!


#### How to run
- `make` for build
- `make load path=<path>` for load and automatically unload after end


#### Example
- `make load path=/home/ubuntu/input.txt` path for input.txt file for Day1


## How its works
 *add git*